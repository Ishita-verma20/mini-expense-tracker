services:
  - type: web
    name: expense-tracker-backend
    env: node
    region: oregon
    root: server
    buildCommand: npm install
    startCommand: npm run start
    plan: free
    autoDeploy: true
